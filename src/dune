(library
  (name RollHash)
  (modules hash)
  (libraries 
    core)
  (preprocess
  (pps ppx_jane)))

(library
  (name BigBWT)
  (modules naive_bwt pfp fm_index)
  (modules_without_implementation fm_index)
  (libraries 
    core
    RollHash)
  (preprocess
  (pps ppx_jane)))

(executable
  (name buildBWT)
  (modules
    buildBWT)
  (libraries
    core
    core_unix.sys_unix
    stdio
    BigBWT)
  (preprocess
    (pps ppx_jane)))
